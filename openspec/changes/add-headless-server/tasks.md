# Tasks

## 1. 外壳骨架与门面

- [x] 1.1 新建 `script/server/init.lua`，建立服务器门面（启动 / 停止 / 建会话 / 销毁会话 / 状态查询），并在 `script/moe-kill.lua` 挂到 `moe.server`
- [x] 1.2 定义"逻辑处理器"的接口约定与校验：必须是 table 且实现约定的入口；缺入口时创建会话必须明确报错
- [x] 1.3 定义"待处理事项"的最小形状（类型标识 + 可选负载表），并写入 `moe-kill-dev` 技能的接口约定说明

## 2. 会话生命周期

- [x] 2.1 实现会话对象与阶段常量（未开始 / 运行中 / 已结束 / 已中止 / 已销毁）及阶段查询
- [x] 2.2 实现建会话：校验处理器、登记当前会话；已存在会话时明确报错（不静默覆盖）
- [x] 2.3 实现 `start`：以协程承载逻辑处理器入口开始运行
- [x] 2.4 实现 `finish` 与 `abort(reason)`：结束与中止，并对非法阶段迁移报错
- [x] 2.5 实现销毁会话：清理登记；对已销毁会话的任何操作报错；重复销毁安全

## 3. 决策通道

- [x] 3.1 实现"请求输入"：登记待处理事项并挂起当前协程（复用 `moe.await`）
- [x] 3.2 实现读取当前待处理事项（没有时返回 nil）
- [x] 3.3 实现提交结果：校验存在等待中的请求 → 唤醒并回传提交值，支持多个值
- [x] 3.4 无等待中的请求时提交 → 报错，不静默忽略
- [x] 3.5 实现决策超时：请求可带超时，超时后唤醒挂起方并抛出超时错误，且会话不再处于等待状态
- [x] 3.6 实现取消：唤醒挂起方并抛出取消错误

## 4. 事件收集

- [x] 4.1 实现事件发出：按发出顺序追加到会话的事件列表
- [x] 4.2 实现事件读取：返回只读快照；未产生事件时返回空集合而不是报错

## 5. 服务模式接线

- [x] 5.1 调整 `main.lua` 的服务模式：启动外壳后常驻事件循环；测试模式分支保持不变

## 6. 测试套件（用假逻辑处理器，不需要任何游戏规则）

- [x] 6.1 新增 `test/server/init.lua` 并挂到测试入口的套件列表
- [x] 6.2 覆盖：非法阶段迁移被拒绝；未挂载处理器创建会话失败
- [x] 6.3 覆盖：单值决策往返；多值决策往返；无等待请求时提交报错
- [x] 6.4 覆盖：决策超时；主动取消
- [x] 6.5 覆盖：事件按顺序记录；空会话读取到空集合
- [x] 6.6 覆盖：重复启停安全；无任何外部客户端时跑通"建会话 → 决策往返 → 产出事件 → 结束"全流程

## 7. 验收与文档

- [x] 7.1 `luamake -notest` 编译通过，`bin/moe-kill.exe --test` 全部通过且退出码为 0
- [x] 7.2 服务模式冒烟：启动进程确认进入就绪状态，且不建立任何对外监听
- [x] 7.3 更新 `moe-kill-dev` 技能的 `references/architecture.md`：补上"逻辑处理器 / 驱动者"的接口约定（含入口必须可被协程承载这一约定）
- [x] 7.4 清理 `tmp/`、`log/` 中的临时产物
