# Tasks

## 1. 事件循环去忙等

- [x] 1.1 `tools/event-loop.lua`：删除 `busyTime` / `markBusy` / `getIdleTime` 与按 `idleTime` 分级的"忙就不睡"逻辑
- [x] 1.2 `tools/event-loop.lua`：`start` 的入参改为注入项 `waiter(seconds)` 与 `deadline()`；默认 waiter 用 `thread.sleep`、默认 deadline 返回 `nil`
- [x] 1.3 `tools/event-loop.lua`：每轮按「下一个到期时间」阻塞等待；没有任何定时任务时无限期阻塞（不周期唤醒）
- [x] 1.4 `tools/timer.lua`：新增"距离最近一个定时任务到期还有多少秒"查询（没有定时任务时返回 `nil`），供循环计算等待时长
- [x] 1.5 等待时长向上取整并留 1 毫秒余量，避免醒来时目标时间未到而多空转一轮
- [x] 1.6 `tools/event-loop.lua`：注入 `waker()`，`stop()` 先请求唤醒再退出，保证阻塞中的等待立即返回

## 2. 异步 I/O 模块（`script/async-io.lua`）

- [x] 2.1 创建 `bee.async` 实例（失败即报错，不静默降级）；提供 `wait(seconds)` 阻塞等待（可被完成事件提前唤醒）
- [x] 2.2 实现完成事件分发 `poll()`：按提交时登记的 `udata` 把结果交回发起方；未登记或重复完成时记日志并忽略，不静默丢弃
- [x] 2.3 实现异步文件读（长度由同步 stat 取得，一次提交读完整个文件；失败以错误形式交回；大文件分批读优先级降低，本次不做）
- [x] 2.4 实现异步文件写（失败以错误形式交回）
- [x] 2.5 实现外部事件源注册（按 fd 提交 poll，完成时回调取走数据、不代调用方 `pop`）；实测确认 `bee.channel():fd()` 可用，不可用则退化为仅提供按 fd 注册的通用入口并记录原因
- [x] 2.6 补齐类型注解与使用说明：句柄关联后必须 `close`、不与同步读写混用同一句柄
- [x] 2.7 实现自唤醒通道：持有线程通道并按 fd 注册为可读事件源；`wake()` 投递一次通知，`poll()` 时清掉通知（不产生虚假唤醒）

## 3. 接线

- [x] 3.1 `script/moe-kill.lua`：挂 `moe.asyncIO`，并向事件循环注入 `waiter`（基于 `bee.async` 的等待）与 `deadline`（基于 `moe.timer`）
- [x] 3.2 `script/moe-kill.lua`：把完成事件分发挂成高优先级任务（每轮先分发完成事件，再跑其它任务）
- [x] 3.3 `test.lua`：删除 sleeper 里 `moe.timer.update(1000)` 的虚拟时间兜底，改走注入的等待
- [x] 3.4 `test/smoke/eventloop.lua`：删除依赖 `getIdleTime` 的"空闲时间随真实时间推进"用例

## 4. 测试（`test/async/`）

- [x] 4.1 新增 `test/async/init.lua` 并挂到测试入口的套件列表
- [x] 4.2 覆盖：空闲约 200 毫秒内"每轮执行"的任务执行次数远低于空转量级（不空转）
- [x] 4.3 覆盖：定时任务按时执行（延迟不超过等待分片上限）
- [x] 4.4 覆盖：没有定时任务时请求停止，循环仍在有限时间内退出并交回控制权；重复停止安全
- [x] 4.5 覆盖：异步写入后读回内容一致；读取不存在的文件以错误交回
- [x] 4.6 覆盖：文件操作期间已注册的"每轮执行"任务仍在被调用（循环未被文件操作阻塞）
- [x] 4.7 覆盖：外部事件源变为可读时循环被唤醒并取到数据；无数据时不产生虚假唤醒
- [x] 4.8 覆盖：以超长等待进入阻塞后收到唤醒请求，等待立即返回（远早于超时）

## 5. 验收与记账

- [x] 5.1 `luamake -notest` 编译通过；`bin/moe-kill.exe --test` 全部通过、退出码 0，且事件循环迭代次数回落、总耗时明显下降
- [x] 5.2 服务模式冒烟：空闲等待期间 CPU 占用可忽略（对比改动前后），进程仍常驻且不建立任何对外监听
- [x] 5.3 技能记账：`moe-kill-dev` 新增「本工程对 `tools/` 的改动清单」，并更新 `references/infrastructure.md` 里的等待 / 唤醒机制说明
- [x] 5.4 清理 `tmp/`、`log/` 中的临时产物
