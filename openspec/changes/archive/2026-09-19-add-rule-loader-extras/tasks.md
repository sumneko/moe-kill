# Tasks

## 1. 互斥声明

- [x] 1.1 `rule.depends` 的项支持 `!` 前缀：解析（复用依赖项的路径解析，含相对路径）与记录（`ctx.exclusions[项] = 声明者`）；验证：互斥声明被正确解析的用例通过
- [x] 1.2 判定「已加载」用带分隔符的前缀匹配（`项 == p` 或 `项 .. '/'` 是 `p` 的前缀；「项是文件」时判等）；验证：`国战` 与 `国战外传` 不互相误判的用例通过
- [x] 1.3 冲突时报错指出**声明者**与**被排斥的项**；该项没被加载时不报错；验证：两个用例都通过
- [x] 1.4 结束前兜底校验（基于实际执行集合）；验证：互斥方在清单**后面**时也能拦住的用例通过

## 2. 预解析

- [x] 2.1 照搬 `sumneko/utility/without-check-nil.lua` 到 `server/tools/`（原样、不改），并在 `infrastructure.md` 登记来源与授权；验证：文件与上游一致，且 `--test` 全绿
- [x] 2.2 新增 `server/rule/preparse.lua`：stub 环境（`rule` 只记依赖 / 互斥 / 条目名）+ 标准库白名单 + `enable()`／`disable()` 成对开关（`<close>` 保证恢复）；验证：试跑不产生副作用、`nil` 元表被恢复的用例通过
- [x] 2.3 迭代展开到不动点（已试跑集合防环）：清单项 → 试跑 → 新依赖项 → 再展开；验证：跨文件依赖在元信息里完整的用例通过
- [x] 2.4 试跑失败记警告并继续（元信息里标记该文件"试跑失败"）；验证：该用例通过

## 3. 元信息与执行前校验

- [x] 3.1 汇总包元信息（包名 / 依赖 / 互斥 / 条目 / 文件与来源 / 试跑状态），随每次 `load` 重建；验证：查询接口返回内容的用例通过
- [x] 3.2 `rule:getPackageMeta(包名)` / `rule:getMetas()` 返回**只读快照**；验证：改动返回值不影响内部状态的用例通过
- [x] 3.3 执行前校验互斥：冲突时**在执行任何文件之前**报错（错误发生时规则表里没有任何条目）；验证：该用例通过
- [x] 3.4 执行前校验同包重复条目（用试跑提取的条目清单），报错指出两处来源；验证：该用例通过

## 4. 测试

- [x] 4.1 新增 `server/test/rule/meta.lua` 并挂到套件列表，覆盖规格里每个 Scenario（互斥三项 + 预解析五项）；验证：`--test rule.meta` 全绿
- [x] 4.2 组合场景：两个互相排斥的包放进清单，断言"报错 + 规则表为空 + 没有任何文件被执行"；验证：该用例通过
- [x] 4.3 回归：既有 `--test rule` / `--test rule.vfs` / `--test rule.routing` / `--test rule.event` 全绿（依赖语义不变）；验证：全绿

## 5. 文档

- [x] 5.1 `architecture.md` 第 9 节：补「互斥声明」（写在 9.3 的清单/依赖项小节里），「预解析与包元信息」另起**第 11 节**（不再塞进第 9 节）
- [x] 5.2 `infrastructure.md`：`server/tools/` 改动清单补 `without-check-nil.lua`（来源 `sumneko/utility`、原样照搬、用途）；命令速查补 `--test rule.meta`
- [x] 5.3 `sanguosha-rules` 的规则集侧写法：补互斥声明（`rule.depends { '!国战' }`）的用法与"模式之间互斥"的约定

## 6. 验收

- [x] 6.1 `luamake -notest` 通过；全量 `--test` 全绿退出码 0；`--test rule.meta` 全绿；问题面板 information 及以上为 0
- [x] 6.2 确认预解析**没有**改变依赖语义（依赖仍在执行到那一行时同步加载，顺序与去重行为不变）
- [x] 6.3 确认 `nil` 的元表在加载结束后是**恢复原状**的（试跑窗口外不留全局改动）
