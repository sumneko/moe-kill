# Tasks

> 前置：变更 `add-package-vfs` 先落地（逻辑路径与来源合并）。

## 1. 规则表与包

- [x] 1.1 规则表改为按包分组：`cards[包名][名字] = 定义` + 包的首次出现顺序表；清空重载时一并清空；验证：`--test rule` 全绿
- [x] 1.2 加载时把「当前文件所属的包」放进加载上下文（由文件逻辑路径的第一层目录推出）；验证：定义自动归属当前包的用例通过
- [x] 1.3 逻辑路径只有一段（根下散落文件）→ 报错，指出规则集文件必须位于包目录内；包目录名含 `.` → 报错；验证：两个用例都通过

## 2. 定义入口与校验

- [x] 2.1 `rule.card` 只在加载过程中可用（加载之外调用报错）；名字里含 `.` 报错；验证：两个用例都通过
- [x] 2.2 同包重复声明报错，报错信息带两处来源（两个文件逻辑路径）；跨包同名不报错；验证：两个用例都通过
- [x] 2.3 定义对象带 `name` / `packageName` / `fullName` / `source`（字段名不用 `package`：LuaDoc 里它是访问修饰符）；验证：元数据用例通过

## 3. 名字路由

- [x] 3.1 包外裸名走默认路由：按包的加载顺序取第一个定义该名字的包；验证：`{ '标准', '军争' }` 下包外查 `杀` 取 `标准.杀` 的用例通过
- [x] 3.2 包内裸名**优先本包**；本包没有该名字时 fallback 到默认路由；验证：两个用例都通过
- [x] 3.3 限定名（`包名.名字`）精确取指定包、不受优先级影响；验证：包内 `军争.杀` 与 `标准.杀` 各取到正确一条且不是同一个对象的用例通过
- [x] 3.4 只有后面的包定义某名字时裸名也可用（如 `火杀`）；验证：该用例通过
- [x] 3.5 裸名与限定名在同一个包的同一个条目上返回**同一个对象**（改一处另一处同步可见）；验证：该用例通过
- [x] 3.6 未登记的名字返回「不存在」；验证：该用例通过

## 4. 测试

- [x] 4.1 既有 `server/test/rule/init.lua` 适配：探针保持「来源目录 / 包目录（`pk`）」结构；「同名得到同一条定义」的断言改为经**查询入口**取（声明入口已不再有「取或建」语义）；验证：`--test rule` 全绿
- [x] 4.2 新增 `server/test/rule/routing.lua` 并挂到套件列表，覆盖规格里每个 Scenario（包名取自一级目录、散落文件报错、包名分隔符报错、自动带前缀、同包重名报错、跨包并存、追加回调用 `getCard`、包外默认路由、包内优先本包、本包没有时 fallback、限定名、只有后者定义时可用、同一对象、来源信息）；验证：`--test rule.routing` 全绿
- [x] 4.3 组合场景：多包清单加载后清空重载，路由结果随清单顺序改变；验证：该用例通过

## 5. 文档

- [x] 5.1 `moe-kill-dev/references/architecture.md` 第 9 节：补「包与名字路由」（包 = 一级目录、同包唯一、裸名 / 限定名、路由优先级），并把「来源合并的后者胜 vs 包路由的清单先者胜」对照表写全
- [x] 5.2 `sanguosha-rules` 的规则集侧写法：补包目录约定、限定名用法、跨文件追加回调用 `rule.getCard` + `rule.depends` 的固定写法、以及「包内裸名优先本包、引用他包同名条目要写限定名」的提醒
- [x] 5.3 `moe-kill-dev/SKILL.md` 的目录职责表提到包与路由，指到第 9 节

## 6. 验收

- [x] 6.1 `luamake -notest` 通过；全量 `--test` 全绿退出码 0；`--test rule` 与 `--test rule.routing` 全绿；问题面板 information 及以上为 0
- [x] 6.2 两个包定义同名条目时裸名与限定名各取到正确的一条，且调整清单顺序后裸名解析结果随之改变 —— 已由 `--test rule.routing` 的「包外裸名走默认路由」「限定名不受包内优先影响」「清单顺序决定裸名解析」三个用例覆盖
- [x] 6.3 确认本批未引入跨包改写能力（没有"替别人的包改定义"的接口）
