# Tasks

## 1. 内核：让出原样上抛（`Effect`）

- [ ] 1.1 `server/core/effect.lua`：`apply()` 的驱动循环改成**透明转发** —— 子执行体让出什么（理由 + 载荷）就原样交给调用方（`coroutine.yield(理由, 载荷)`），调用方恢复时给的值原样送进子执行体；验证：`--test core.effect` 既有取消/退栈/嵌套用例全绿
- [ ] 1.2 `server/core/effect.lua`：取消的让出带上「是哪个效果」（`yield('cancelled', self)`），驱动循环只处理载荷为自己的那一个、其余原样上抛；未知理由当场报错；验证：新增用例「嵌套里取消内层，外层照常」+ 既有取消用例全绿
- [ ] 1.3 `server/core/game.lua`：局上记住**当前挂起的询问**（最多一个）+ 挂起期间任何结算入口（`play` / `damage` / `ask`）MUST 明确失败；验证：新增用例「挂起期间起新结算被拒绝」，断言挂起的结算与结算栈都不受影响

## 2. 内核：询问的挂起、答复与超时

- [ ] 2.1 `server/core/ask.lua`：`settle()` 让出 `('ask', self)`，答案取恢复值；三种结果用哨兵区分 —— 有答案 ⇒ 写入 `self.answer`；回答者显式「拿不出答案」⇒ 明确失败（保留现有错误口径）；超时 ⇒ 以「没有答案」正常结束；验证：`--test core.response` 既有用例 + 新增三例各自通过
- [ ] 2.2 `server/core/ask.lua` + `server/core/game.lua`：询问带**时限**（`game:ask(to, question, options?)` 的 `timeout`，未给用约定值）与「它现在是不是挂着」的只读查询；验证：新增用例「发起方选定的时限生效」+ 「重复答复不改写答案」
- [ ] 2.3 `server/core/effect.lua`：**挂起态取消要连带收掉它下面挂着的子结算**（沿结算栈自顶向下依次关闭）；验证：新增用例「取消一个挂着子结算的挂起效果 ⇒ 栈回到结算前、不留孤儿」
- [ ] 2.4 `server/core/use-card.lua` / `server/core/damage.lua` / `server/core/game.lua`：结算入口与 `settle` 标 `---@async`，并确认逐目标循环在前一个生效（含它的挂起）结束后才处理下一个；验证：`--test core.play` 既有用例全绿 + 新增用例「逐目标：第一个挂起不影响第二个的处理顺序」

## 3. 测试侧驱动器与假等待器

- [ ] 3.1 `server/test.lua`：测试模式给事件循环注入**假 `waiter`**（推虚拟时间：`moe.timer.update(...)`，照 4.0.0 的 `ls.timer.update(1000) + thread.sleep(1)`）；验证：既有全量用例照常全绿、总耗时没有明显上升
- [ ] 3.2 测试侧**驱动器**（跑流程协程、按让出理由服务：`'ask'` ⇒ 把询问连同答复入口交给回答者 + 起超时定时器，答复到达或超时后把值送回去）；验证：新增用例「挂起后控制权确实交回驱动器（流程还没结束）」
- [ ] 3.3 `server/test/rule/support.lua`：脚本化回答者改成**在返回前调答复入口**（`fun(ask, resume)` 形状）；验证：`--test rule.slash` 与 `--test rule.setup` 全绿（用例体不动）

## 4. 无头用例：挂起与超时的行为

- [ ] 4.1 「挂起 → 当场答复」：调用链不产生等待（回答者返回前用答复入口），行为与改动前一致；验证：断言结算结束后状态、栈都正确
- [ ] 4.2 「挂起 → 稍后答复」：回答者留着入口，稍后（虚拟时间里过一段）才送答案；验证：发起方从挂起处继续并读到答案，其间流程确实处于挂起
- [ ] 4.3 「挂起 → 超时」：没人答复，时限到点后以「没有答案」结束；验证：发起方照常继续、不报错，且这次询问的答案字段为空
- [ ] 4.4 「挂起 → 被取消」：挂起期间取消这次询问；验证：答案字段为空、发起方照常继续（复用既有取消口径）
- [ ] 4.5 「回答者拿不出答案 ⇒ 明确失败」：验证：以明确失败暴露，而不是静默当成「被问者拒绝」
- [ ] 4.6 【杀】端到端：一个目标挂起后稍后打出闪（不受伤）＋ 另一个目标超时（受伤）；验证：`--test rule.slash` 新增用例通过
- [ ] 4.7 新增/改动的套件在 `server/test.lua` 里**显式登记**（`test.require`），避免"新增用例没被跑到"；验证：全量用例数比改动前多出新增条数

## 5. 文档

- [ ] 5.1 `moe-kill-dev` 的 `references/architecture.md`：§12 补「让出原样上抛 + 最外层服务（驱动器）」的机制说明与让出理由表；验证：与代码一致，`kind`/理由名不出现凭空的名字
- [ ] 5.2 `moe-kill-dev` 的 `references/code-style.md`：补两条约定 —— ① 效果执行体 MUST 原样转发让出；② `await.*` 只能在最外层（驱动器）用，嵌套层要等就让出上抛（否则搁浅）；验证：新写法在文档里有例子
- [ ] 5.3 `sanguosha-rules` 的 §7：补「响应可以挂起（等答复）与超时」的口径；验证：与 `core-response` 的规格一致

## 6. 验收

- [ ] 6.1 全量验证：`luamake -notest` 通过；`server\bin\moe-kill.exe --test` 全绿；问题面板 information 及以上为 0；`openspec validate add-effect-suspend --strict` 通过
