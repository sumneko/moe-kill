# Tasks

## 1. 内核：让出的转发与效果完成信号（`server/core/effect.lua`）

- [x] 1.1 `apply()` 的驱动循环改成**透明转发**：子执行体让出什么（理由 + 载荷）就原样交给调用方，调用方恢复时给的值原样送进子执行体；验证：`--test core.effect` 既有取消/退栈/嵌套用例全绿（行为不变）
- [x] 1.2 让出理由分派：`'cancelled'` 由它自己的驱动点收尾（取消只可能由该效果自己让出，所以不需要带载荷），`'wait'` 原样上抛，**未知理由当场报错**；验证：既有取消用例全绿（「取消只作用于这一个效果，外层照常结算完」覆盖了嵌套情形）
- [x] 1.3 效果变成**可等待的任务**（用户 2026-09-20 定）：完成信号 + 结果与失败记在 `result` / `err` 上；`:apply()` 驱动并返回效果自己、`:wait()` 等它结完并返回效果自己（幂等）；叶子的挂起点是 `Effect:suspend(请求)`（载荷原样交给外头，内核不解释）；验证：新增用例「等它结完并拿到结果」、「已经结完的效果再等立刻返回」、「失败同时抛出来并记在效果上」

## 2. 内核：挂起期间的状态约束

- [x] 2.1 `server/core/game.lua`：记录「正在等外部输入」（`game:enterWaiting()` 返回撤销，计数器挂在局上），挂起期间任何结算入口（`play` / `damage` / `ask`）MUST 明确失败（拦在 `pushEffect` 这一处）；验证：新增用例「挂起期间不能起新结算」，断言挂起的结算与结算栈都不受影响
- [x] 2.2 `server/core/effect.lua`：**挂起态取消要连带收掉它下面的子结算**（沿结算栈自顶向下依次关闭）；验证：新增用例「取消挂起的结算连带收掉子结算」——栈回到这次结算之前、不留孤儿
- [x] 2.3 `server/core/game.lua`：**用牌 / 伤害入口自动「驱动 + 等」并返回效果实例**（`moe.damage.create{...}:apply():wait()`），`settle` 与入口标 `---@async`；验证：新增用例「结完的效果再 wait 立刻返回」断言入口返回的伤害已结完，`--test core.play` / `--test core.damage` 全绿、面板 0

## 3. 测试侧驱动者与假等待器

- [x] 3.1 **不注入假等待器**（用户的本意是「等待类用例不真等」，实际实现后发现它不需要）：ⓘ `add-async-io` 那批已经**特意删掉**测试里的虚拟时间兑底（那是为了掩盖忙等而"提前"触发定时器），且 `test/async/idle.lua` 用 `moe.timer.clock()` 断言真实延迟，注入假等待器会动到它；② 挂起是**控制流**、与时间无关 ⇒ 模拟"稍后"只要让出一次循环 tick（`moe.await.sleep(0)`）；验证：全量用例 0 失败、耗时与改动前相当
- [x] 3.2 测试侧**驱动者** `server/test/driver.lua`：建流程协程、按让出理由服务（`'wait'` ⇒ 交给测试给的服务函数，服务函数就跑在最外层、可以 `await`），把结果送回去；验证：新增用例「挂起时控制权交回调用方」与「结果能穿过多层嵌套下传」
- [x] 3.3 新增用例都落在**已登记**的套件 `test.core.effect` 里（驱动者是辅助模块，由套件 `require`，不是套件所以不必登记）；验证：全量用例数 292 → 299（+7）

## 4. 无头用例：挂起与恢复的行为

- [x] 4.1 「不挂起时行为不变」：既有 292 个用例全绿就是这条的验证（结算体不调用 `wait` 时调用链一次跑完）
- [x] 4.2 「挂起 → 当场给结果」：服务函数返回即结果；验证：用例「等它结完并拿到结果」断言结果记在 `result` 上、`err` 为空、栈恢复
- [x] 4.3 「挂起 → 稍后给结果」：服务函数先让出一次循环 tick 再给结果；验证：用例「稍后给结果（跨过一次事件循环）」断言外头等着时确实还没结果
- [x] 4.4 「挂起 → 被取消」：用例「取消挂起的结算」与「取消挂起的结算连带收掉子结算」；验证：它不再继续、栈回到结算前、不漏孤儿
- [x] 4.5 回归确认：`server/core/ask.lua` 本批**没有任何改动**（`git diff` 可见），`Ask` 与【杀】/【闪】链路照旧全绿

## 5. 文档

- [x] 5.1 `moe-kill-dev/references/architecture.md`：§2 挂起模型改成「让出理由 + 逐层原样上抛 + 恢复打在最外层」，§12 新增「效果即可等待的任务」条目（完成信号 / `.result` / `.err` / 入口自动驱动+等 / 叶子 `suspend` / 驱动者 / 节流约束 / 取消连带子结算）
- [x] 5.2 `moe-kill-dev/references/code-style.md`：新增「让出要原样转发；`await.*` 只能在最外层用」

## 6. 验收

- [x] 6.1 全量验证：`luamake -notest` 通过；`server\bin\moe-kill.exe --test` **299 用例 0 失败**；问题面板 0；`openspec validate add-effect-suspend --strict` 通过
