# Tasks

## 1. 内核改名与门面

- [ ] 1.1 `script/engine/` → `script/core/`（`git mv`），`---@class Engine` → `---@class Core`
- [ ] 1.2 `script/moe-kill.lua`：`moe.engine` → `moe.core`，类型注解同步
- [ ] 1.3 文档修正：`moe-kill-dev` 技能的分层图与目录职责表（`engine` → `core`，规则层另立）；`setup-backend-infra/design.md` 里过期的"`script/engine/` 未来放纯规则引擎"一句

## 2. 随机源（`core-random`）

- [ ] 2.1 `script/core/random.lua`：按 seed 构造的可实例化伪随机源（取范围整数 / 取元素 / 洗牌），不触碰全局 `math.random` 状态
- [ ] 2.2 相同 seed 产生相同序列；两个实例互不干扰

## 3. 牌堆（`core-piles`）

- [ ] 3.1 `script/core/pile.lua`：基类（放入 / 取出 / 查看 / 计数 / 是否为空；对空堆取牌等非法操作明确失败）
- [ ] 3.2 抽牌堆：按固定顺序取顶 + 消费注入的随机源洗牌
- [ ] 3.3 弃牌堆：接受放入并计数，与抽牌堆相互独立
- [ ] 3.4 处理区：临时牌区（放入 + 整体清空），清空不影响其它牌区
- [ ] 3.5 `script/core/card.lua`：牌实例（唯一标识 + 标签，不含任何牌定义）

## 4. 对象模型（`core-objects`）

- [ ] 4.1 玩家：体力与体力上限（受 0 ~ 上限约束）、存活、手牌区 / 装备区 / 判定区（放入 / 取出 / 计数 / 查询）、不透明标签
- [ ] 4.2 桌子：入座、座位序号、按行动顺序遍历（跳过已阵亡）、查询相邻座位
- [ ] 4.3 座位距离求值：沿两侧取较小值、最小为 1、修正参与求值且修正后仍最小为 1（求值非缓存）
- [ ] 4.4 房间：加入玩家、持有桌面与各牌堆、查询当前局面；两个房间互不影响

## 5. 测试（`test/core/`）

- [ ] 5.1 新增 `test/core/init.lua` 并挂到测试入口的套件列表
- [ ] 5.2 覆盖随机源：同种子同序列、不同种子不同序列、实例间互不干扰
- [ ] 5.3 覆盖牌堆：放入 / 取出 / 计数、空堆取牌明确失败、相同随机源洗出相同顺序、按顺序取牌、弃牌堆与处理区
- [ ] 5.4 覆盖对象：体力上下限、牌区操作与计数、标签、行动顺序跳过阵亡、座位距离与下限、两个房间互不影响
- [ ] 5.5 覆盖"调用方组合接口搭场景"：用手动取牌 + 加手牌的方式发出一手牌，断言玩家手牌与抽牌堆计数 —— 证明内核不提供"发牌"也能搭出场景

## 6. 验收与文档

- [ ] 6.1 `luamake -notest` 编译通过；`bin/moe-kill.exe --test` 全绿、退出码 0；问题面板 information 及以上归零
- [ ] 6.2 `architecture.md`：分层图补 `core`（内核，与规则无关）与规则层（后续批次）；「无头可测」补"内核接口可直接调用、调用方自行组合场景"
- [ ] 6.3 `sanguosha-rules` 技能：把「决策点统一走请求输入 → 挂起 → 恢复」的主语改为 **Room 内部**（后续批次实现），并注明**内核不含任何流程**
- [ ] 6.4 清理 `tmp/`、`log/` 中的临时产物
