# Tasks

## 1. 基础件：随机源、牌实例、门面

- [ ] 1.1 `script/engine/random.lua`：按 seed 构造的**可实例化**伪随机源（取范围整数 / 取元素 / 洗牌），不触碰全局 `math.random` 状态
- [ ] 1.2 `script/engine/card.lua`：牌实例（唯一标识 + 供测试断言的标签；不含任何牌定义）
- [ ] 1.3 `script/moe-kill.lua`：`moe.engine` 挂上各接口，并补齐类型注解

## 2. 牌堆（`card-piles`）

- [ ] 2.1 `script/engine/pile.lua`：牌堆基类（放入 / 取出 / 查看 / 计数 / 是否为空；对空堆取牌等非法操作明确失败）
- [ ] 2.2 抽牌堆：按固定顺序取顶 + 用注入的随机源洗牌（相同随机源状态洗出相同顺序）
- [ ] 2.3 弃牌堆：接收弃置的牌并计数，与抽牌堆相互独立
- [ ] 2.4 处理区：结算临时区（放入 + 整体清空），清空不影响其它牌区

## 3. 对象模型（`game-objects`）

- [ ] 3.1 玩家：身份 / 体力 / 体力上限 / 存活 / 手牌区 / 装备区 / 判定区；体力增减受上下限约束
- [ ] 3.2 桌子：入座、座位序号、按行动顺序遍历（跳过已阵亡）、查询相邻座位
- [ ] 3.3 座位距离求值：沿桌面两侧取较小值、最小为 1、含修正且修正后仍最小为 1（求值而非缓存字段）
- [ ] 3.4 房间：加入玩家、持有桌面与各牌堆、查询当前局面；不同房间状态互不影响

## 4. 决策接口（`engine-decisions`）

- [ ] 4.1 定义 `Engine.Decider` 契约（同步签名，请求形状 `{ kind, payload? }`，返回值支持多个）；未注入决策者时进入决策点必须明确报错
- [ ] 4.2 测试用脚本化决策者：预置答案队列（按序弹出）、支持多值、支持返回错误

## 5. 开局装配（`game-setup`）

- [ ] 5.1 身份配置表（4~10 人，取自 `sanguosha-rules`）
- [ ] 5.2 身份分配：主公唯一、其余身份数量与表一致、相同随机源状态可复现
- [ ] 5.3 洗牌与发牌：每名玩家起手牌数一致；已发出的牌 + 抽牌堆剩余 = 装配前总数
- [ ] 5.4 默认口径：主公体力上限 +1、先手为主公（默认值可配置）

## 6. 回合阶段（`turn-flow`）

- [ ] 6.1 阶段枚举与时机名（每个阶段的开始 / 结束都是可枚举的时机名）
- [ ] 6.2 单步推进接口 + 当前阶段 / 当前回合角色的查询
- [ ] 6.3 回合交接：结束阶段走完后交给行动顺序中的下一位存活玩家，阶段重置为准备阶段
- [ ] 6.4 摸牌阶段默认摸两张（手牌 +2、抽牌堆 -2）
- [ ] 6.5 弃牌阶段：手牌数超过当前体力时通过决策接口提出 `discard` 请求并应用结果，引擎不代选
- [ ] 6.6 进入需要决策的阶段但未注入决策者时，以明确错误暴露（不静默跳过、不阻塞）

## 7. 测试（`test/engine/`）

- [ ] 7.1 新增 `test/engine/init.lua` 并挂到测试入口的套件列表
- [ ] 7.2 覆盖牌堆：放入 / 取出 / 计数、空堆取牌明确失败、相同 seed 洗牌一致、按顺序取牌、弃牌堆与处理区
- [ ] 7.3 覆盖对象：体力上下限、区域操作、行动顺序跳过阵亡、座位距离与修正下限、两个房间互不影响
- [ ] 7.4 覆盖装配：四人局身份构成、相同 seed 可复现、起手牌数一致与牌守恒、主公体力上限 +1 与先手
- [ ] 7.5 覆盖流程：阶段顺序与单步推进、回合交接、时机名可枚举、摸两张、弃牌走决策接口、未注入决策者报错
- [ ] 7.6 覆盖可复现：相同随机源 + 相同决策序列得到相同局面（阶段、体力、各牌区内容）

## 8. 验收与文档

- [ ] 8.1 `luamake -notest` 编译通过；`bin/moe-kill.exe --test` 全绿、退出码 0；问题面板 information 及以上归零
- [ ] 8.2 `sanguosha-rules` 技能：把"决策点统一走请求输入→挂起→恢复"的主语改为 **Room 内部**，并注明引擎侧通过注入决策者接口（本批为脚本化应答者）
- [ ] 8.3 `architecture.md`：分层与「无头可测」补上"引擎接口可直接调用、决策依赖注入"
- [ ] 8.4 清理 `tmp/`、`log/` 中的临时产物
