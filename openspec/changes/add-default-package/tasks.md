# Tasks

## 1. 虚拟文件系统（`server/rule/vfs.lua`）

- [ ] 1.1 来源目录名以 `@` 开头时剥离 `@` 得到逻辑第一层名字，并把这个逻辑包记为「默认加载」；目录名只有 `@`（后面没名字）时报错；验证：`--test rule.vfs` 新增用例通过
- [ ] 1.2 暴露默认加载包名单（去重、按逻辑名升序，供加载器使用）；无标记来源时返回空表；验证：该用例通过
- [ ] 1.3 同一个逻辑包在多个来源里、只有其中一个带标记时也算默认加载（标记只增不减）；验证：该用例通过

## 2. 加载器（`server/rule/init.lua`）

- [ ] 2.1 `load` 把默认加载的包**排在清单项之前**自动加入（默认包之间按逻辑名升序），再按既有规则执行（重复引用只执行一遍）；验证：`--test rule` 新增用例通过
- [ ] 2.2 显式把默认包写进清单时仍然只执行一遍；验证：该用例通过
- [ ] 2.3 引用面全部用不带 `@` 的逻辑名（依赖项 / 限定名 / `getPackageMeta` 的包名）；验证：该用例通过
- [ ] 2.4 `loadFile` 校验逻辑路径的**非首段**不得以 `@` 开头，命中时报错并指出该逻辑路径；验证：该用例通过

## 3. 仓库落地

- [ ] 3.1 `package/基础` 改名为 `package/@基础`（目录改名，文件内容不动）；`package/身份场/开局.lua` 的 `rule.depends { '../基础' }` 保持原样；验证：`--test rule.identity` 与 `--test rule.setup` 仍全绿（它们依赖默认加载）
- [ ] 3.2 既有规则侧套件的清单去掉 `基础`（`server/test/rule/{base,identity,setup}.lua` 与 `support.lua`）；验证：三个套件全绿且用例语义不变

## 4. 测试补充

- [ ] 4.1 `server/test/rule/init.lua`：默认包不在清单里也会被加载、且排在清单之前；验证：用例通过
- [ ] 4.2 `server/test/rule/vfs.lua`：`@` 剥离、默认名单、`@` 后无名字报错；验证：用例通过
- [ ] 4.3 `server/test/rule/routing.lua` 或 `init.lua`：非首段出现 `@` 时加载报错（探针包里放 `@配置.lua`）；验证：用例通过

## 5. 文档

- [ ] 5.1 `architecture.md` 第 9 节：9.2 / 9.3 补「默认加载标记（`@` 前缀的来源目录名）」，并说明顺序与引用口径；9.1 的落点示例同步
- [ ] 5.2 `moe-kill-dev/SKILL.md` 目录表：`package/` 行改成现有 `@基础` / `身份场` / `标准`，并写明 `@` 的含义
- [ ] 5.3 `sanguosha-rules` 第 9.1 节：包名一节补 `@基础` 与「依赖写 `../基础`、不写 `@`」
- [ ] 5.4 `code-style.md` 第 8 节命名表的「包名与包内文件名」一行补一句：包目录名可带 `@` 前缀表示默认加载，包内文件名不带

## 6. 验收

- [ ] 6.1 `luamake -notest` 通过；全量 `--test` 全绿退出码 0；问题面板 information 及以上为 0
- [ ] 6.2 `openspec validate --all --strict` 全通过
