# Tasks

## 1. 内容侧：两张牌

- [x] 1.1 `package/标准/卡牌/桃园结义.lua`（新）：顶部写官方描述；`Card '桃园结义' : extends '锦囊牌'`；`'获取目标'` 返回 `game.desk.alivePlayers`（含自己）；`'生效'` 里 `game:heal(ctx.target, 1)`（满体力者回复 0 点 —— 官方「对未受伤的角色无效」）；**不声明 `limit`**；验证：`--test rule.trick` 全绿
- [x] 1.2 `package/标准/卡牌/决斗.lua`（新）：顶部写官方描述；`Card '决斗' : extends '锦囊牌'`；`'获取目标'` 返回其他存活角色（`table.filter` 排掉使用者）；`'生效'` 里由**目标**先开始轮流 `game:askCard(当前, '打出', { name = '杀' })`，谁没打出来谁受**另一方**造成的 1 点伤害（`game:damage(另一方, 未打出者, 1)`）；验证：`--test rule.trick` 全绿

## 2. 用例（`server/test/rule/trick.lua`）

- [x] 2.1 桃园结义：受伤的每人回 1 点（含使用者自己）、满体力的不变、用过的牌进 `弃牌`；验证：`--test rule.trick` 全绿
- [x] 2.2 桃园结义：合法目标是所有存活角色且**含自己**（`canUse` 第三条返回值）；验证：同上
- [x] 2.3 决斗：**先问目标**（把被问顺序记下来断言）、双方各打一张后目标打不出 ⇒ 目标挨 1 点且来源是使用者、打出的【杀】进 `弃牌`；验证：同上
- [x] 2.4 决斗：目标手上没【杀】⇒ 直接挨 1 点；合法目标不含自己；验证：同上
- [x] 2.5 分类：把两张新牌并入既有的分类循环用例（`isKind('锦囊')` / `isKind('非延时锦囊')` / 分类列表 / 从 `手牌` 用）；验证：同上

## 3. 文档与验收

- [x] 3.1 `.agents/skills/sanguosha-rules/SKILL.md`：§9.10 补两张牌的官方原文与已实现口径（"未受伤无效"落到回复 0 点、"由目标先开始轮流打【杀】、未打出者受另一方 1 点"）、§4 的牌分类表（已落地清单）与 §7 的「还没做」同步（剩【五谷丰登】【过河拆桥】【顺手牵羊】【借刀杀人】【无懈可击】）
- [x] 3.2 `.agents/skills/moe-kill-dev/references/progress.md`：内容包现状（可用的锦囊从 3 张变 5 张）、§2 候选表（「其它普通锦囊」那条改成剩余 5 张 + 五谷丰登要的内核口子）
- [x] 3.3 验收：`server/bin/moe-kill.exe --test` 全量 0 失败、问题面板 information 及以上 0；提交（`【AI】` 前缀）
